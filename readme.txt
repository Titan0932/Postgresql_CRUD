

run the virtual env:
    venv/scripts/activate

install dependencies:
    pip install -r requirements.txt

rename ".database_sample.ini" to "database.ini"
enter the DB host, dbname, username and password